[project]
name = "mintclip"
version = "0.1.0"
description = "A verifiable clipboard manager"
requires-python = ">=3.8"
dependencies = [
    "typer",
    "pyperclip",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py38"
